# saena

saena is opt-in. Enable with `grok mcp enable saena`. When the
server is connected, call `status` first, claim work, complete
when done. The host snapshots changes. Do not ask for SAENA_DESK
or file paths.

This checkout is project `saena`. The tree is the home desk.
`saena surface` is the verb list if you are in a shell.
